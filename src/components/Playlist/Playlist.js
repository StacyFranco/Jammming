import React from 'react';
import Tracklist from '../Tracklist/Tracklist';

function Playlist () {
    return(
        <div>
            <Tracklist />
        </div>
    );
};

export default Playlist;